# Loop Agent

This toy example shows how to iteratively create a report a domain based on the google search results.

# Architecture 

Sequential(Loop(basic_domain_agent -> advanced_domain_agent) -> report_agent)
